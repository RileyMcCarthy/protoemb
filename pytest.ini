[pytest]
# Pin the rootdir to the library so `tests/conftest.py` (which puts core/ and the
# testkit on sys.path) loads no matter where pytest is invoked from.
testpaths = tests
python_files = test_*.py
addopts = -ra
